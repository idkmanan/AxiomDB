# Phase 3 — query plans at 1000005 rows

Generated by `benchmarks/scripts/explain.mjs`. Every number is the median of 3
runs of `EXPLAIN (ANALYZE, BUFFERS, VERBOSE)`; the full plans are in the
`explain-*.txt` files beside this one.

**Deep page, OFFSET vs keyset: 7473.6x** — `offset-deep` and `keyset-deep` return the
same 20 rows at depth 950000.

| query | ms (index) | ms (no index) | ratio | buffers (index) | buffers (no index) | Sort node | Seq Scan |
|---|---|---|---|---|---|---|---|
| `offset-deep` | 657.677 | — | — | 1907180 | — | no | no |
| `keyset-deep` | 0.088 | 210.541 | 2392.5 | 46 | 73545 | no | no |
| `keyset-first-page` | 0.079 | 409.619 | 5185.1 | 10 | 73905 | no | no |
| `keyset-owner-scoped` | 0.129 | 43.234 | 335.1 | 8 | 41708 | no | no |
| `keyset-open-stage-partial` | 0.084 | 11.38 | 135.5 | 8 | 21187 | no | no |
| `pipeline-summary-groupby` | 209.098 | — | — | 97955 | — | yes | no |
| `exact-count-star` | 302.479 | — | — | 72982 | — | no | yes |

"no index" means the index named in the script was dropped inside a transaction that was
then rolled back — the query is measured with it genuinely absent. A dash means no index
was dropped for that query.

The `Sort node` column is the one to read for finding F-47: a Sort above a scan on a
keyset query means the ORDER BY did not match the index and Postgres sorted the whole
result instead of walking it in order.

## Environment

PostgreSQL 16.4 on x86_64-pc-linux-musl, compiled by gcc (Alpine 13.2.1_git20240309) 13.2.1 20240309, 64-bit

| setting | value |
|---|---|
| effective_cache_size | 98304 8kB |
| jit | on |
| max_parallel_workers_per_gather | 2 |
| random_page_cost | 4 |
| shared_buffers | 32768 8kB |
| track_io_timing | on |
| work_mem | 8192 kB |

Table: heap 110 MB, indexes 95 MB, exact rows
1000005, `reltuples` estimate 1000005.
