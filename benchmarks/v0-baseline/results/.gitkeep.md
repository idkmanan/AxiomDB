# Placeholder — no results committed yet

This directory is intentionally empty.

Real k6 output lands here when `./benchmarks/scripts/run-baseline.sh` is executed
on a machine with Docker and k6. No estimated, synthetic, or placeholder figures
are committed, because a fabricated baseline would defeat the entire purpose of
Phase 0.

To populate it:

```bash
cp .env.bench.example .env.bench
git tag v0-baseline
npm run bench:baseline
npm run bench:report
```

Expected files afterwards:

```
v0-asbuilt-vus100.json     v0-nolimit-vus100.json
v0-asbuilt-vus500.json     v0-nolimit-vus500.json
v0-asbuilt-vus1000.json    v0-nolimit-vus1000.json
v0-saturation-rate50.json  v0-saturation-rate200.json  v0-saturation-rate500.json
*.summary.json             (k6 --summary-export)
```

Plus, one level up: `environment.json`, `pg_stat_statements.txt`,
`explain-users-list.txt`, and the generated `SUMMARY.md`.
